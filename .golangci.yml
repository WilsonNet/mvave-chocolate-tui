linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell

run:
  timeout: 2m
  tests: true

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
  exclude-dirs:
    - cmd
